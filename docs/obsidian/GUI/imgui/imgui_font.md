[[imgui]]

# Glyph not found !

```c++
// It is actually in the font?
if (!stbtt_FindGlyphIndex(&src_tmp.FontInfo, codepoint))    
	continue;
```

* そもそもフォントに含まれていない(fontforgeで調べれ)
- 含まれていても見つからない場合？

# Clear

# Size

# Add
